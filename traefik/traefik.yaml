entryPoints:
  web:
    address: ":80"
    http:
      redirections:
        entryPoint:
          to: websecure
          scheme: https
  websecure:
    address: ":443"

api:
  dashboard: true

providers:
  docker:
    exposedByDefault: false

certificatesResolvers:
  le:
    acme:
      email: "im.aufa.ai@gmail.com"  # Replace with your email
      storage: "/data/acme.json"
      tlsChallenge: {}